#!/bin/sh

set -e

declare -a list_of_files=(
  "bin"
  ".git"
  ".gitconfig"
  ".githelpers"
  ".gitmodules"
  "README.md"
  "scripts"
  ".tmux.conf"
  ".vim"
  ".vimrc"
  ".zlogin"
  ".zlogout"
  ".zpresto"
  ".zshenv"
  ".zshrc"
)

if [ "$#" -gt 0 ] && [ "$1" = "-v" ]; then
  verbose=true
else
  verbose=false
fi

echo "==> Deleting files..."

for file in "${list_of_files[@]}"; do
  if $verbose; then
    echo "====> Deleting $file..."
  fi

  rm -fr "$file"
done

echo "==> Done Uninstalling..."
